{
  "name": "squid",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "rm -rf lib && tsc",
    "update": "npx npm-check-updates --filter /subsquid/ --upgrade && npm i -f"
  },
  "dependencies": {
    "@subsquid/archive-registry": "^2.1.7",
    "@subsquid/evm-processor": "^0.4.0",
    "@subsquid/gsheets-store": "^0.0.1",
    "dotenv": "^16.0.3",
    "ethers": "^5.7.2",
    "google-auth-library": "^8.7.0"
  },
  "devDependencies": {
    "@subsquid/evm-typegen": "^2.1.1",
    "@types/node": "^18.11.18",
    "typescript": "^4.9.4"
  }
}
